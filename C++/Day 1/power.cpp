#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int a, b, num;
    cout << "Enter the two binary no \n"
         << endl;
    cin >> a >> b;

    num = (pow(a,b));
    cout<<"The Power of the number sum is : \n";
     cout << num;

}